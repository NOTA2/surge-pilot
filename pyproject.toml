[project]
name = "surge-pilot"
version = "0.1.0"
description = "Research-first US momentum trading system"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
surge-pilot = "surgepilot.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
